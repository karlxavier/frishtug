(function() {
  $('#menu-item-modal .modal-body')
  .html('<%= j render 'form_modal', menu: @menu, menu_category: @menu_category %>');
  $('#menu-item-modal .modal-title')
    .html('New <%= @menu_category.name.humanize.capitalize %> Menu Item')
  $('#menu-item-modal').modal('show');
  // dropzone_init();
  diet_category_init();
  init_dropzone();
}())
